# Direct API publishing is intended for development before a domain is ready.
# Keep the loopback default when Caddy provides HTTPS on the same host.
API_PUBLISH_HOST=127.0.0.1
API_PORT=48180

# Set this after a domain resolves to the server, then start the full stack.
DOMAIN=api.example.com
